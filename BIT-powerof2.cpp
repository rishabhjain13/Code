#include<bits/stdc++.h>
using namespace std;

#define endl "\n"

bool isPowerOfTwo(int n) {
        cout <<(n & (n-1));
        return ~(n & (n-1));
    }

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    
}